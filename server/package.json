{
  "name": "westore-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:all": "jest --watch",
    "test:unit": "jest --watch --testPathIgnorePatterns ./integration",
    "test:integration": "jest --watch ./integration"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mongodb": "^3.3.0-beta2",
    "properties-reader": "^0.3.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "jest": "^23.6.0",
    "jest-mock-express": "^0.1.1"
  },
  "jest": {
    "setupFiles": [ "./testsetup.js" ]
  }
}
